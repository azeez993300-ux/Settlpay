{
  "name": "settl",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":   "cd backend && yarn dev",
    "start": "cd backend && yarn start",
    "setup": "cd backend && yarn install"
  },
  "engines": { "node": ">=18" }
}
